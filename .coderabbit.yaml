# CodeRabbit configuration for automatic reviews on all Pull Requests
version: "1"

reviews:
  # Enables automatic reviews on all target branches using a wildcard
  target_branches:
    - "*"
  
  # Automatically triggers reviews upon creation or updates of any PR
  auto_review:
    enabled: true

  # Disables status messages unless a critical issue occurs
  review_status: false
